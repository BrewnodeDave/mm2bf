[build]
  publish = "webapp/static"
  functions = "netlify/functions"
  command = "npm install && cd webapp && npm install"
  base = "."

[dev]
  publish = "webapp/static"
  port = 8888
  autoLaunch = true
  framework = "#static"
